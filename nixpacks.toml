[phases.setup]
nixPkgs = ["python311", "gcc", "postgresql"]

[phases.install]
cmds = ["python3 -m venv .venv && .venv/bin/pip install --no-cache-dir -r requirements.txt"]

[phases.start]
cmd = ".venv/bin/python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT --workers 1"
